version: 2.1 # Use version 2.1 to enable orb usage.

orbs:
  heroku: circleci/heroku@0.0.10 # The Heroku orb give you everything you need to start using the Heroku executor.

workflows:
  heroku_deploy:
    jobs:
      - heroku/deploy-via-git